A Meteor package for debugging objects in templates.

---

### Install

`meteor add tomba:json-helpers`

### Usage

Use the json helper to debug objects in templates:

```handlebars
{{json myObject}}
```
